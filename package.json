{
  "name": "servicenow-server-scoped-dts",
  "version": "0.1.0",
  "description": "JavaScript Typings for ServiceNow server-side scoped application development",
  "main": "app.js",
  "author": {
    "name": "Leonard T. Erwine"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@types/node": "^8.0.14",
    "typescript": "^3.8.2"
  }
}
